function cocktails_PAGINATE() {
  // DEFINE VARIABLES
  var cocktailsDIV = document.querySelector('.cocktails-container');
  var paginateDIV = document.querySelector('.cocktails-paginate');

  // UPDATE
  // cocktailsDIV.innerHTML = "<%= j render (partial: 'cocktails') %>";
  // paginateDIV.innerHTML = "<%= j paginate @cocktails, remote: true %>";

  console.log(paginateDIV);
}

cocktails_PAGINATE();